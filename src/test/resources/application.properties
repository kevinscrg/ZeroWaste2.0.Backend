spring.application.name=ZeroWaste2.0.Backend

spring.datasource.url=jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

server.port=${SERVER_PORT:8080}
server.address=${SERVER_ADDRESS:0.0.0.0}

frontend.url=${FRONTEND_URL:http://localhost:8100/}

security.jwt.access.secret=${JWT_SECRET:bgfyidsvfhduosafvy9oe_hobiuofafsdafsa}
security.jwt.refresh.secret=${JWT_REFRESH:hufgirpasf8vbr92ybv4y301pbc[aJS[HPF]}
security.jwt.access.expiration-ms=${JWT_EXPIRATION_MS:3600000}
security.jwt.refresh.expiration-ms=${JWT_EXPIRATION_REFRESH_MS:1209600000}

spring.mail.host=${MAIL_HOST:localhost}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:test@example.com}
spring.mail.password=${MAIL_PASSW:password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000




